import React from "react";
import MapContainer from "../components/MapContainer";
type Props = {};

function test({}: Props) {
  return (
    <div>
      <MapContainer />
    </div>
  );
}

export default test;
